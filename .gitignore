# Back up .env securely. It contains private keys.                                                                                    │
# Add .env to .gitignore — never commit secrets to version control.
.env

# Per-chain mint server secrets (docker compose); see mint.*.env.example
mint.*.env

# also exposes stuff, used by mock wallet client
.ghost_wallet.json

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.cursorignore

# Virtual environments
.venv

# js stuff
node_modules
